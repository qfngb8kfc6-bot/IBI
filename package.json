{
  "name": "ibi-company-finder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A lightweight browser-based finder for the IBI company research database.",
  "scripts": {
    "import:data": "node scripts/import-data.mjs",
    "dev": "node scripts/server.mjs",
    "start": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node --check public/app.js && node --check scripts/import-data.mjs && node --check scripts/build.mjs && node --check scripts/server.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}

